[tool.ruff]
target-version = "py310"
line-length = 100
select = [
    "E",
    "F",
    "I",
    "UP",
    "B",
    "PIE",
    "RET",
]
ignore = [
    "E203",
]

[tool.ruff.per-file-ignores]
"tests/*" = ["S101"]

[tool.pytest.ini_options]
pythonpath = ["."]
addopts = "-q"

